# Expanded VFDB
This repository contains the protein sequences and Foldseek database for AlphaFold (v4) proteins predicted to be a bacterial virulence factor

`afdb_to_vfdb_mapping.tsv` maps a query VFDB ID to an AlphaFold protein ID.
- Two proteins being mapped means that they both have similarity in structure and genomic context.
